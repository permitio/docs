---
sidebar_position: 1
title: Projects & Environments
description: "Understand how Permit.io organizes policy and data into workspaces, projects, and environments, and manage projects, environments, their IDs, API keys, and member access in the dashboard."
---

import createProject from "/ui-videos/project-and-env/creating-a-new-proj.mp4";
import editFilterDeleteProj from "/ui-videos/project-and-env/editing-filtering-deleting-proj.mp4";
import manageAccessProj from "/ui-videos/project-and-env/manage-access-proj.mp4";

import editEnvironment from "/ui-videos/workspace-settings/editing-env.mp4";
import manageAccessEnv from "/ui-videos/project-and-env/manage-access-env.mp4";

Learn how Permit.io organizes your authorization setup into a workspace, projects, and environments, and how to manage projects and environments in the Permit dashboard. This page is for admins and developers who set up a Permit workspace.

An environment is a silo (a logical grouping) of your policy (roles, resources, condition sets) and data (users, tenants, role assignments). Each project can have several environments. For example, you can create a `production` environment for your production deployment and a `staging` environment for your staging deployment, and use environments in CI/CD flows and for testing.

## Working with the Permit hierarchy \{#working-with-the-permit-hierarchy}

The Permit object model has three levels:

| Level | What it holds | Typical mapping |
|---|---|---|
| Workspace (organization) | All of your projects, members, and API keys | Your company |
| Project | A set of environments | One application or service that needs its own policy |
| Environment | One policy and its data | One deployment of the application, such as `dev`, `staging`, or `production` |

![Permit object model: a workspace contains projects, each project contains environments, and each environment contains tenants, users, roles, and resources](/img/diagrams/api-model.svg)

Each environment contains:

- Tenants: isolated groups of users and resources, usually one per customer of your application
- Users: the identities you check permissions for
- Your policy and roles
- Resources and actions: the objects in your application that you control access to, and the operations on those objects

Within an environment, role assignments link users to tenants. For example, user `alice` has the role `admin` in tenant `my-customer`. The same user can belong to several tenants in the same environment. See [Multi-tenant authorization](/concepts/multi-tenant-authorization).

## Projects

A project groups the environments of one application or service that needs its own policy and roles. A workspace can have several projects, one for each product, service, or tool you offer. A new workspace includes a project named **Default Project**.

Manage projects from the **[Projects](https://app.permit.io/projects)** screen of the Permit dashboard.

![Projects screen of the Permit dashboard with project cards and their environments](/ui-videos/project-and-env/projects-and-envs.png)

### Create a project \{#creating-a-new-project}

On the **Projects** screen, create a project for each application or service that needs its own policy.

<video controls>
  <source src={createProject} />
</video>

### Edit, filter, and delete projects \{#editing-filtering-and-deleting-projects}

On the **Projects** screen, filter the list of projects, and edit or delete a project.

<video controls>
  <source src={editFilterDeleteProj} />
</video>

### Get the project ID \{#getting-the-project-id}

Most calls to the Permit API require a project ID and an environment ID. To get the project ID in the dashboard, click the three dots on the project card. The project key appears in the menu. To get project and environment IDs with the API, see [Get the project ID and environment ID](/api/examples/get-project-and-env).

![Project card menu in the Permit dashboard showing the project key](/ui-videos/project-and-env/project-id.png)

### Manage access to a project \{#managing-access-to-a-project}

Invite a member to one project to give the member access to that project only. For the member roles you can assign, see [Member management](/manage-your-account/workspace-settings#member-management).

<video controls>
  <source src={manageAccessProj} />
</video>

## Environments

Within a project, create the environments you need, such as `dev`, `staging`, and `production`.

:::info
When you switch the active environment in the dashboard, the Policy Editor shows the policy of the newly active environment. Switching the active environment doesn't affect API calls.
:::

Every environment has its own API key. Your SDK, the Permit API, and your policy decision point (PDP) use the environment API key to check permissions in that environment. From the dashboard you can copy and rotate the environment API key, edit or delete the environment, and switch the active environment.

Manage environments from the **[Project](https://app.permit.io/project-management)** screen of the Permit dashboard.

![Environment cards of a project in the Permit dashboard](/ui-videos/project-and-env/manage-envs.png)

### Create an environment \{#creating-a-new-environment}

Create an environment from scratch in the dashboard or with the API, or copy an existing environment. See [Create, copy, and merge environments](/manage-your-account/creating-environments).

### Get the environment ID \{#getting-the-environment-id}

To get the environment ID in the dashboard, click the three dots on the environment card. The environment key appears at the top of the menu. To get project and environment IDs with the API, see [Get the project ID and environment ID](/api/examples/get-project-and-env).

![Environment card menu in the Permit dashboard showing the environment key](/ui-videos/workspace-settings/getting-env-id.png)

### Copy and rotate the environment API key \{#fetching-and-rotating-the-api-key}

1. On the environment card, click the three dots.
2. Click **Copy API Key** to copy the environment API key, or **Rotate API Key** to replace the key with a new one.

![Environment card menu with the Copy API Key and Rotate API Key options highlighted](/ui-videos/workspace-settings/fetch-and-rotate-api-key.png)

:::warning Rotating an API key revokes the old key
After you rotate an environment API key, the old key stops authenticating. SDK clients and PDPs that still use the old key can't reach Permit until you configure them with the new key.
:::

For more on API keys, see [Get your API key](/overview/get-api-key).

### Edit and delete environments \{#editing-and-deleting-environments}

On the environment card, click the three dots, then click **Edit Environment** to change the environment details.

<video controls>
  <source src={editEnvironment} />
</video>

To delete an environment, click **Delete Environment** in the same menu.

![Environment card menu with the Delete Environment option highlighted](/ui-videos/workspace-settings/delete-env.png)

### Manage access to an environment \{#managing-access-to-an-environment}

Invite a member to one environment to give the member access to that environment only. For the member roles you can assign, see [Member management](/manage-your-account/workspace-settings#member-management).

<video controls>
  <source src={manageAccessEnv} />
</video>

## Next steps

- [Create, copy, and merge environments](/manage-your-account/creating-environments)
- [Manage workspace members and API keys](/manage-your-account/workspace-settings)
- [Manage workspaces, projects, and environments with the API](/manage-your-account/workspace-api)
- [Get your API key](/overview/get-api-key)
