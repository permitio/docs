---
sidebar_position: 2
title: Workspace Settings
description: "Invite and manage the members of your Permit.io workspace, assign workspace, project, and environment roles, manage API keys, and request a Data Processing Addendum."
---

import memberManagement from "/static/ui-videos/workspace-settings/member-management.mp4";
import invitingUsers from "/static/ui-videos/workspace-settings/inviting-users.mp4";
import changingMemberRoles from "/static/ui-videos/workspace-settings/changing-member-roles.mp4";

import createNewKey from "/static/ui-videos/workspace-settings/creating-new-key.mp4";
import rotatingAndDeletingKey from "/static/ui-videos/workspace-settings/rotating-and-deleting-key.mp4";

Manage who can access your Permit.io workspace, which API keys can call the Permit API, and your data processing terms. This page is for workspace owners who administer a Permit workspace.

Members are the people on your team who use the Permit dashboard or API. Members are different from users, the identities your application checks permissions for.

## Member management

Member roles control which members can change policies, in the Permit dashboard or through the API, and in which projects and environments.

To manage members:

1. In the Permit dashboard sidebar, open **Settings**.
2. Select the **Members** tab.

<video controls>
	<source src={memberManagement} />
</video>

### Understanding member roles

When you invite a member to your workspace, you assign the member one of these workspace roles:

| Workspace role | Create roles, resources, and policies | Change workspace settings | Invite members and change their access |
|---|---|---|---|
| Workspace Owner | Yes | Yes | Yes |
| Workspace Editor | Yes | No | No |
| Workspace Viewer | No (view only) | No | No |

A member can have access to the entire workspace, or only to specific projects and environments:

- To give a member different permissions in one project or environment, invite the member directly into that project or environment with a role there, such as Project Owner or Environment Editor. The member's workspace role stays the same everywhere else.
- If you invite a member only into specific projects or environments, and not into the workspace, the member has access to those projects and environments only. When those roles differ in access level, the **Access Level** column of the Members tab shows `Mixed`.

#### Example: a member with mixed access \{#lets-look-at-an-example}

In this example, a member has no workspace role. The member has two roles scoped to projects:

- **Owner** of the `Shopping App` project, in all of its environments
- **Editor** of the `dev` environment of the `Internal App` project

The Members tab shows **2 Projects** in the **Allow Access To** column and `Mixed` in the **Access Level** column. Click `Mixed` to see the role in each project.

![Members tab with a member set to Mixed access, showing Owner of Shopping App in all environments and Editor of the dev environment of Internal App](/ui-videos/workspace-settings/mixed-access-level.png)

### Adding new members

Invite members one at a time, or invite several members in one bulk invitation. A bulk invitation assigns the same role to every member in the invitation. To invite some members as Workspace Owners and others as Workspace Editors, send one invitation for each role.

<video controls>
	<source src={invitingUsers} />
</video>

### Changing member roles

On the **Members** tab, change the role of a member in the workspace, and in each project and environment the member belongs to. For a member who has access only to specific projects or environments, click **Give access to entire workspace** to assign a Workspace Owner, Workspace Editor, or Workspace Viewer role.

<video controls>
	<source src={changingMemberRoles} />
</video>

### Removing a member

On the **Members** tab, open the access level menu of the member and click **Remove from workspace**.

![Access level menu of a member on the Members tab with the Remove from workspace option highlighted](/ui-videos/workspace-settings/delete-member.png)

### Sending an invitation link

When you invite a member, Permit emails the member a link to accept the invitation. Until the member accepts, the Members tab shows the member as a blank user.

![Members tab with a pending invitation shown as a blank user](/ui-videos/workspace-settings/user-invitation-pending.png)

If the member didn't receive the email, copy the invitation link and send the link to the member yourself:

1. On the **Members** tab, open the access level menu of the member.
2. Click **Copy Invite Link**.

Each invitation link is unique to one invited member.

![Access level menu of a member on the Members tab with the Copy Invite Link option highlighted](/ui-videos/workspace-settings/copying-invite-link.png)

## API keys

API keys authenticate calls to the Permit REST API at the organization (workspace), project, or environment level.

To manage API keys:

1. In the Permit dashboard sidebar, open **Settings**.
2. Select the **API Keys** tab.

### Organization vs Project vs Environment keys

| API key level | Scope | Use it for |
|---|---|---|
| Organization API key | The entire workspace: all projects and environments | Workspace-level actions, such as creating projects |
| Project API key | One project and all of its environments | Project-level actions, such as creating environments |
| Environment API key | One environment | Your application, SDKs, and policy decision point (PDP). Only an environment API key works with the PDP container. |

Give each system the lowest level of API key that covers its tasks. Anyone with an organization API key can change every project and environment in your workspace through the Permit API.

:::note
Permit creates an environment API key for every new environment.
:::

For how to get an API key and call the Permit API, see [Get your API key](/overview/get-api-key) and [Use the Permit API](/api/api-with-cli).

### Creating new keys

On the **API Keys** tab, create an API key at the level you need.

<video controls>
	<source src={createNewKey} />
</video>

### Rotating and deleting keys

On the **API Keys** tab, rotate or delete an API key.

:::warning Rotating or deleting an API key revokes the old key
After you rotate or delete an API key, the old key stops authenticating. Every application, SDK client, PDP, and script that uses the old key fails to call Permit until you configure the new key.
:::

<video controls>
	<source src={rotatingAndDeletingKey} />
</video>

## Data Processing Addendum (DPA)

Permit offers a standard [Data Processing Addendum](https://www.permit.io/legal/data-processing-addendum) that you can review and adopt. Enterprise plan customers and Pro plan customers (for an additional fee) can request a tailored DPA from Permit support or their customer success manager. See the [Permit.io pricing page](https://www.permit.io/pricing) for plan details.

Permit doesn't require personally identifiable information (PII) in its control plane. The only required field of a user is the user key, so you can use opaque user identifiers as user keys and omit email addresses and names.

## Next steps

- [Understand projects and environments](/manage-your-account/projects-and-env)
- [Audit changes with the activity log and API log](/manage-your-account/permit-logs)
- [Manage members and keys with the API](/manage-your-account/workspace-api)
