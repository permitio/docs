---
title: "GitOps commands in the Permit CLI"
sidebar_label: "GitOps"
description: "Reference for the Permit CLI commands that connect a GitHub repository to a Permit environment for GitOps and clone the policy repository."
---

Connect a GitHub repository to a Permit.io environment and clone the policy code that Permit writes to the repository, with the `permit gitops` commands. This reference is for developers who manage their policy as code in Git, or extend the generated policy with custom Rego.

Before you use these commands, install the Permit CLI and sign in with `permit login`. See [Install and use the Permit CLI](/how-to/permit-cli). For how GitOps works in Permit, see [GitOps overview](/integrations/gitops/overview).

## Sync policies to a Git repository \{#sync-policies-to-git-repositories}

### `permit gitops create github`

`permit gitops create github` starts a wizard that connects a GitHub repository to your Permit environment. The wizard runs these steps:

1. Asks you to select a project.
2. Asks for a repository key, which names the repository configuration in Permit.
3. Generates an SSH key pair and copies the public key to your clipboard.
4. Asks you to add the public key as a deploy key of the repository at `https://github.com/<organization>/<repository>/settings/keys/new`, then asks for the repository's SSH URL.
5. Asks for the branch name.
6. Saves the configuration in Permit.

| Flag | Alias | Default | Description |
|---|---|---|---|
| `--inactive` | `-i` | Not set | Saves the configuration without activating the repository after Permit validates the repository. |
| `--api-key <string>` | `-k` | Stored credentials | An API key of the project or organization. |

Connect a repository and leave it inactive:

```bash
$ permit gitops create github --inactive
```

Without `--inactive`, the CLI prints that GitOps is configured and activates once Permit validates the repository. With `--inactive`, the CLI prints that GitOps is configured, and you activate the repository later. For the repository setup and deploy key steps in GitHub, see [GitOps with GitHub](/integrations/gitops/github).

### `permit gitops env clone`

`permit gitops env clone` runs `git clone` on the policy repository of the active GitOps configuration. By default, the CLI lists the environments of the project, and clones the branch of the environment you select. With `--project`, the CLI clones the whole repository with every branch.

| Flag | Default | Description |
|---|---|---|
| `--project` | Not set | Clones the whole repository instead of one environment branch. |
| `--dry-run` | `false` | Prints the `git clone` command without running it. |
| `--api-key <string>` | Stored credentials | A project API key. |

For one environment, the CLI clones only the `permit/generated/<environment-id>` branch. To see the `git clone` command before the CLI runs it, pass `--dry-run`. When the clone finishes, the CLI prints a success message, and the repository is in a new directory under your current directory.

## Extend the generated policy with custom Rego \{#extend-predefined-policies-with-custom-rego-open-policy-agent}

After you clone the policy repository, you can add custom Open Policy Agent (OPA) Rego rules to the generated policy. For the files to change and a worked example, see [Write a custom policy with GitOps](/integrations/gitops/custom_policy).

To print the policies that a running OPA server has loaded, use [`permit opa policy`](/how-to/permit-cli/permit-cli-policy#opa-policy).

## Related pages

- [GitOps overview](/integrations/gitops/overview)
- [GitOps with GitHub](/integrations/gitops/github)
- [Manage environments with the Permit CLI](/how-to/permit-cli/permit-cli-envs)
- [Permit CLI command index](/how-to/permit-cli#full-command-list)
