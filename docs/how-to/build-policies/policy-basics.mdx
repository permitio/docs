---
sidebar_position: 1
title: Policy Basics
description: "Learn the building blocks of a Permit.io policy (resources, actions, roles, users, and tenants) and where you create each one in the Permit dashboard."
---

import createRole from "/static/ui-videos/policy-basics/creating-a-role.mp4";
import createResource from "/static/ui-videos/policy-basics/creating-a-resource.mp4";
import createUser from "/static/ui-videos/policy-basics/creating-a-user.mp4";
import assignMultipleRoles from "/static/ui-videos/policy-basics/assigning-multiple-roles.mp4";
import givePermissions from "/static/ui-videos/policy-basics/giving-permissions-to-role.mp4";
import createTenant from "/static/ui-videos/policy-basics/create-a-new-tenant.mp4";
import switchTenant from "/static/ui-videos/policy-basics/switching-between-tenants.mp4";
import switchView from "/static/ui-videos/policy-basics/changing-policy-view.mp4";

Learn the building blocks of a Permit.io policy (resources, actions, roles, users, and tenants) and where you create each one in the Permit dashboard. This page is for developers who are new to Permit and want to understand the model before they build their first policy.

You define the policy schema (resources, actions, and roles) on the **Policy** screen, which includes the [Policy Editor](https://app.permit.io/policy-editor). You add the data the policy evaluates (users, tenants, and role assignments) on the **Directory** screen. Permit generates policy code from what you configure, and your policy decision points (PDPs) evaluate that code when your application calls `permit.check()`.

The first sections define each building block. [Do each task in the Permit dashboard](#using-the-low-code-policy-editor) then lists the steps for each dashboard task, with a video of the same flow.

## What is a policy? \{#what-is-a-policy}

A policy is the set of rules that decides whether a user can perform an action on a resource.

- The most basic policy consists of **roles**, **actions**, and **resources**: a role is allowed to perform an action on a resource.
- A policy belongs to an **environment**. Each environment in a project has its own policy.

## Supported authorization models \{#supported-authorization-models}

A Permit policy can grant permissions in three ways, and you can combine them in one policy:

| Model | Grants permissions based on | Learn more |
|---|---|---|
| Role-based access control (RBAC) | The roles assigned to the user | [RBAC overview](/how-to/build-policies/rbac/overview) |
| Relationship-based access control (ReBAC) | The user's relationship to a specific resource instance | [ReBAC overview](/how-to/build-policies/rebac/overview) |
| Attribute-based access control (ABAC) | Attributes of the user, resource, or tenant | [ABAC overview](/how-to/build-policies/abac/overview) |

To compare the models and see how they combine, read [Mix and Match Policies](/how-to/build-policies/overview).

## Define resources and actions \{#define-resources-and-actions}

Resources and actions are the base of every policy. You define them on the **Resources** tab of the Policy screen, or through the Permit API.

### Resources

A resource is an object type you want to control access to, such as a document, a board, or an API endpoint. Resources belong to an environment.

For example, in a social media application, the resources could be user profiles, posts, comments, and private messages.

### Actions

An action is an operation a user can perform on a resource. Each resource has its own list of actions, so an action such as `sign` can exist on one resource only.

For example, in a music streaming application, the actions on a `song` resource could be `play`, `skip`, `add_to_playlist`, `download`, and `share`.

### How roles, resources, and actions form permissions

In the Policy Editor, each role has a checkbox for every action of every resource. When the checkbox is selected, users with that role can perform that action on that resource.

![Policy Editor block view for the Admin role with checkboxes for the actions of the Board, Document, and Task resources](/ui-videos/policy-basics/admin-policy.png)

The screenshot shows the `admin` role of a policy with three resources: `Board`, `Document`, and `Task`. The `admin` role holds every action on `Board`, `create` and `update` on `Document`, and `read` on `Task`. The `sign` action exists on `Document` only.

### Default permissions

When you create a resource in the Policy Editor, Permit adds a default set of actions to the resource and grants default permissions to default roles. The **Default Permissions** panel of the environment holds those defaults. The panel in the screenshot below holds these values:

| Role | Actions |
|---|---|
| `admin` | `create`, `read`, `update`, `delete` |
| `editor` | `create`, `read`, `update` |
| `viewer` | `read` |

The panel is editable, so the defaults in an environment can differ from this table. To read or change them, click **Settings** in the [Policy Editor](https://app.permit.io/policy-editor). You can edit the default actions and roles, or turn off **Assign the Default Permissions to every new resource**.

![Default Permissions settings panel with the delete, read, update, and create actions, the admin, editor, and viewer roles, and the toggle that assigns default permissions to every new resource](/img/default-setting-form.png)

## Roles and permissions \{#create-roles-top-level-in-the-ui-and-permissions}

A policy grants permissions to roles, and you assign roles to users per tenant.

### Roles \{#roles-top-level-in-the-ui}

A role is a named collection of permissions that you assign to users. Instead of granting permissions to each user, you grant them to a role and assign the role to users. For example, a company application could have `manager`, `engineer`, and `hr` roles, each with a different set of permissions.

Roles are defined per environment: you create them on the **Roles** tab of the Policy screen, or through the Permit API. Each environment has its own set of roles.

The roles described on this page are **top-level roles**: a user holds a top-level role across a whole tenant. The Directory screen shows them in the **Top Level Access** column. For roles on a specific resource instance, see [Resource roles](/how-to/build-policies/rebac/overview#resource-roles).

You assign roles to users on the **Directory** screen, per tenant.

## Manage users and tenants \{#manage-users-and-tenants}

On the [Directory](https://app.permit.io/user-management) screen, you can:

- Create, update, and delete users.
- Assign roles to users.
- Add users to tenants.

You can also manage users and tenants with the Permit API and SDKs. See [Sync users](/how-to/sync-users).

:::note Users and members
Users are the identities you check permissions for, such as the people who sign in to your application. Members are your team in the Permit dashboard, managed in [workspace settings](/manage-your-account/workspace-settings#member-management).
:::

### Users

A user is an identity that your application checks permissions for. A user is usually a person, and can also be a service or an automated process.

Each user has a unique key. You use the key to identify the user in `permit.check()`. A user can also have an email, a first name, a last name, and attributes.

A user belongs to one or more tenants, and has role assignments in each tenant. The same user can hold different roles in different tenants. The **Users** tab of the Directory screen lists each user with its key and the roles it holds in the selected tenant.

### Tenants

A tenant is an isolated group of users and resources inside an environment. In a multi-tenant application, each tenant usually represents one of your customers (the company that you sell to).

Role assignments are scoped to a tenant. A user with the `admin` role in tenant `acme` doesn't have that role in tenant `globex` unless you assign it there too. To use the same user in several tenants, add the same user key to each tenant.

To learn more, see [Multi-tenant authorization](/concepts/multi-tenant-authorization).

## Do each task in the Permit dashboard \{#using-the-low-code-policy-editor}

Each task below lists the steps in the Permit dashboard, followed by a video of the same flow. The tasks are independent, so you can run the one you need. For an end-to-end policy with a worked example, follow [Building an RBAC policy](/how-to/build-policies/rbac/building-rbac-policy) or [Building your first ABAC policy](/how-to/build-policies/abac/building-abac-policy).

### Create a role \{#creating-a-role-top-level-in-the-ui}

1. Open the **Policy** screen and select the **Roles** tab.
2. Create a role. The **New Role** panel opens.
3. Enter a name, such as `Admin`. Permit fills **Key** from the name. Use the key in your code and in API calls.
4. Click **Save**. The role appears in the list on the **Roles** tab.

<video controls>
  <source src={createRole} />
</video>

### Create a resource \{#creating-a-resource}

1. Open the **Policy** screen and select the **Resources** tab.
2. Click **Add Resource**. The **New Resource** panel opens.
3. Enter a name, such as `document`. Permit fills **Key** from the name.
4. In **Actions**, type each action and press Enter after each one. An action that you type without pressing Enter is not added.
5. Click **Save**. The Policy Editor then shows a checkbox for each action of the resource, under every role.

<video controls>
  <source src={createResource} />
</video>

### Add a user and assign a role \{#adding-a-user-and-assigning-a-role-top-level-in-the-ui}

1. Open the **Directory** screen and select the **Users** tab.
2. Click **Add user**. The **Create a new user** panel opens.
3. Enter a **Key** that identifies the user in `permit.check()`. The email, first name, and last name are optional.
4. Select the **Tenant** the user belongs to, such as **Default Tenant**.
5. Under **Top Level Access**, add each role the user holds in that tenant.
6. Click **Save**. The user appears on the **Users** tab with its key, and its roles in the **TOP LEVEL ACCESS** column.

For the same task with screenshots and a worked example, see [Create a user and assign the Admin role](/how-to/build-policies/rbac/building-rbac-policy#creating-a-user--assigning-a-role).

<video controls>
  <source src={createUser} />
</video>

### Assign multiple roles to a user \{#assigning-multiple-roles-top-level-in-the-ui-to-a-user}

A user can hold several top-level roles in the same tenant.

1. On the **Users** tab of the **Directory** screen, open the three-dot menu of the user and select **Edit**.
2. Under **Top Level Access**, add each role the user needs.
3. Click **Save**. The **TOP LEVEL ACCESS** column of the user lists every role you assigned.

<video controls>
  <source src={assignMultipleRoles} />
</video>

### Give permissions to a role \{#giving-permissions-to-a-role-top-level-in-the-ui}

1. Open the **Policy** screen and select the **Policy Editor** tab.
2. Select the checkbox of each action the role can perform on a resource.
3. Click **Save Changes** in the bar that counts your unsaved changes. To drop the changes instead, click **Reset**.

<video controls>
  <source src={givePermissions} />
</video>

### Create a tenant \{#creating-a-new-tenant}

1. Open the **Directory** screen and click **Settings**.
2. Select **Manage Tenants** under **General**, then add a tenant. The **Tenant** dialog opens.
3. Enter a **Name**, and optionally a description. To set tenant attributes, click **Edit Tenant Attributes**.
4. Click **New Tenant**. The tenant appears in the list with the key Permit generated from the name.

<video controls>
  <source src={createTenant} />
</video>

### Switch between tenants \{#switching-between-tenants}

A user can have different roles in each tenant, so switching tenants on the Directory screen shows different role assignments for the same user.

1. Open the **Directory** screen and select the **Users** tab.
2. Open the tenant selector at the top of the screen. A new environment has one tenant, **Default Tenant**, whose key is `default`.
3. Select a tenant. The **Users** tab lists the users who hold a role in that tenant, and the **TOP LEVEL ACCESS** column shows the roles they hold there.

<video controls>
  <source src={switchTenant} />
</video>

### Switch the Policy Editor view \{#switching-the-policy-editor-view}

The Policy Editor has two views: a block view with one card per role, and a grid view with one column per role.

1. Open the **Policy** screen and select the **Policy Editor** tab.
2. Click the grid view button or the block view button in the top-right corner, next to **Create**.

In the grid view, click a resource name to expand its actions. The checkbox in the resource row covers every action of the resource, and shows a dash when the role has only some of them.

<video controls>
  <source src={switchView} />
</video>

### Confirm the policy works \{#confirm-the-policy-works}

Run a permission check for the role, resource, and user you created. The following check asks whether the user `john@permit.io` can perform `read` on `document`:

```javascript
const permitted = await permit.check("john@permit.io", "read", "document");

console.log(permitted);
```

The script prints `true` when `john@permit.io` holds a role that permits `read` on `document` in the tenant of the check:

```text
true
```

For the SDK client setup and the rest of the check options, see [Check permissions with permit.check()](/how-to/enforce-permissions/check). When a check returns `false` and you expected `true`, see [Fix a no_permission denial](/how-to/use-audit-logs/errors/no_permission).

## Policy code generated by Permit \{#permit--cedar-policies}

Permit generates policy code from what you configure in the Policy Editor. The PDP runs Open Policy Agent (OPA) with the **[Rego](https://www.openpolicyagent.org/docs/latest/policy-language/)** language, or **[Cedar](https://www.cedarpolicy.com/en)**. See an [example of generated Rego code](https://github.com/permitio/generated-policy-example/tree/main/permit/generated/conditionset).

Open Policy Administration Layer (OPAL) delivers policy updates from the Permit control plane to each PDP you run. To learn how this works, see [Control & Data planes](/concepts/control-plane-and-data-plane).

:::tip Fun fact

AWS open-sourced the Cedar policy language and authorization engine on May 10, 2023, under the Apache License 2.0. Read the [AWS announcement](https://aws.amazon.com/about-aws/whats-new/2023/05/cedar-open-source-language-access-control).

:::

## Manage policy as code with GitOps \{#gitops}

With the GitOps feature, Permit saves the policy code it generates to a Git repository that you own, before the code is deployed to your PDPs. You can then use your existing Git workflow on policy changes: version history, code reviews, tests, and approvals. You can also add your own Rego code next to the generated code.

To set up GitOps, see [Git and Permit](/integrations/gitops/github). For an overview, see [GitOps overview](/integrations/gitops/overview).

## Next steps

- [Building an RBAC policy](/how-to/build-policies/rbac/building-rbac-policy): create roles, a resource, permissions, and a user.
- [Building your first ABAC policy](/how-to/build-policies/abac/building-abac-policy): create user sets and resource sets from attributes.
- [Building ReBAC policies](/how-to/build-policies/rebac/building-rebac-policies): define relations and role derivations.
- [Check permissions with permit.check()](/how-to/enforce-permissions/check): enforce the policy in your code.
