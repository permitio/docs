---
sidebar_position: 5
title: ABAC Design Patterns
description: "Implement common attribute-based access control (ABAC) patterns in Permit.io: ownership, group membership, and dynamic attributes computed with custom Rego code."
---

Implement common authorization patterns with attribute-based access control (ABAC) in Permit.io: ownership, group membership, and dynamic attributes computed with custom Rego code. This page is for developers who know how to create user sets and resource sets, and want a recommended way to model a specific rule. To create user sets and resource sets, see [Building your first ABAC policy](/how-to/build-policies/abac/building-abac-policy).

## Ownership

With ownership, a user can perform actions only on the resources they own. You can implement ownership in Permit in three ways with the Policy Editor, or write the policy as code. The three ways are equally valid:

| Approach | Where ownership is stored | Use when |
|---|---|---|
| [Ownership via tenants](#ownership-via-tenants) | The tenant of the user and of the resource | Each owner already maps to a tenant |
| [Ownership via a list on the resource](#ownership-via-list-on-resource) | A resource attribute that lists the owner keys | Your application knows a resource's owners when it runs the check |
| [Ownership via a list on the user profile](#ownership-via-list-on-user-profile) | A user attribute that lists the resource keys the user owns | Your application stores ownership on the user |

To model ownership with relationship-based access control (ReBAC) instead, see [Model ownership](/how-to/ownership).

### Ownership via tenants

A user owns a resource when the user and the resource belong to the same tenant. Permit implements this as an ABAC policy for you: pass the tenant in `permit.check()`, and the check allows the action only when the user has a role in that tenant.

To learn how tenants isolate users and resources, see [Multi-tenant authorization](/concepts/multi-tenant-authorization).

### Ownership via list on resource

The resource has an attribute that lists the keys of the users who own it. A resource set checks whether the list contains the key of the user who makes the request.

1. Add an array attribute to the resource, for example `owners`.
2. Create a resource set with the condition: the owners attribute `array contains (ref)` `user.key`.

![Dynamic Resource (Resource Set) form named owned_file for the file resource type, with the condition resource.owner array contains (ref) user.key](/img/resource_ownership.png)

The screenshot names the attribute `owner`. The attribute key in the resource set must match the key you pass in the check.

3. Grant the resource set its actions in the Policy Editor.

To check permissions, pass the owners list as a resource attribute in `permit.check()`. In the following example, `userKey` is the key of the user who makes the request:

```javascript
const permitted = await permit.check(userKey, "read", {
  type: "file", // The resource name
  attributes: {
    owners: [
      "d08c85a349994aeb89a3f02c08bdb340", // user-1
      "48fb889360604253a5189580b48694cf", // user-2
    ],
  },
});
```

The check returns `true` when `userKey` is one of the keys in `owners`.

### Ownership via list on user profile

The user has an attribute that lists the keys of the resources the user owns. A [user set](/how-to/build-policies/abac/building-abac-policy#step-3-defining-dynamic-role-user-set) checks whether that list contains the key of the requested resource.

## Belonging to a group

Permit supports groups natively with ReBAC. To assign roles to a group instead of to each member, use the [Groups API](/api/rebac/groups) or read the [ReBAC overview](/how-to/build-policies/rebac/overview#organizations).

You can also represent a group with ABAC, as a tenant or as an attribute of users and resources.

### Group by tenants

Use tenants as groups, so several tenants together form a higher-level group. Group the tenants with [tenant attributes](/how-to/build-policies/abac/defining-attributes#define-stored-tenant-attributes).

### Group by attributes

Assign users and resources to a group with an attribute such as `group_id` or `group_name`. The implementation follows the [ownership via a list on the resource](#ownership-via-list-on-resource) pattern: compare the group attribute of the resource with the group attribute of the user.

## Combining your own OPA Rego code with Permit.io generated ABAC code

Add custom Open Policy Agent (OPA) Rego code to compute **dynamic attributes** that your ABAC policies use like any other attribute.

### How it works

1. You write **custom Rego code** that generates attributes.
2. You use the generated attributes in user set and resource set conditions, as you use stored attributes.
3. The policy decision point (PDP) runs your custom code with the ABAC code that Permit generates.

### Example: Creating a dynamic intersection attribute \{#example-creating-a-dynamic-intersection-attribute}

The following example computes a `has_intersection` user attribute that is `true` when a list attribute on the user and a list attribute on the resource share at least one value.

Add the code to the `custom/` directory of your [GitOps](/integrations/gitops/overview) repository:

```rego
package permit.custom
import future.keywords.in

# Default value if no conditions are met
default custom_user_attributes["has_intersection"] = false

# Set to true if there's an intersection between user and resource attributes
custom_user_attributes["has_intersection"] := true {
    # Convert attributes to sets for intersection testing
    user_attr_set = {x | some x in data.users[input.user.key].attributes["attr_in_user"]}
    res_attr_set = {x | some x in input.resource.attributes["attr_in_resource"]}
    # Check if there's at least one common value
    count(user_attr_set & res_attr_set) > 0
}
```

The code reads `attr_in_user` from the stored attributes of the user, and `attr_in_resource` from the resource attributes in the check input.

:::info Requirements for custom attribute code

- The package name **must** be `permit.custom`.
- The rule name **must** be `custom_user_attributes`.
- There are no other requirements. The rule body can contain any Rego logic.
:::

### Using the custom attribute

Use `user.has_intersection` in a user set condition, and pass the resource attribute in the check:

```javascript
const permitted = await permit.check(userKey, "read", {
  type: "document",
  attributes: {
    attr_in_resource: ["tag1", "tag2"],
  },
});
```

The check returns `true` when the user's `attr_in_user` list contains `tag1` or `tag2`, and the user set with the `has_intersection` condition has `read` permission on `document`.

### Testing your custom code \{#testing-your-custom-code}

1. **Test in the OPA Playground.** [Open this example in the OPA Playground](https://play.openpolicyagent.org/p/115vpZj3ow) to iterate on the Rego logic without a PDP.

2. **Test with a local PDP.** Run a PDP container. Replace `permit_key_XXXXX` with your environment API key. The command maps the PDP API to port `7767` on your machine, and the OPA API to port `8181`.

   ```bash
   docker run -it -p 7767:7000 -p 8181:8181 -e PDP_API_KEY=permit_key_XXXXX permitio/pdp-v2:latest
   ```

   With the PDP running, you can:

   - Send **POST requests** with your input data to `http://localhost:8181/v1/data/permit`, and read the raw output, including **policy debug data**.
   - **Update a custom policy directly** at `http://localhost:8181/v1/policies/custom/example.rego`.

:::tip Development workflow

1. Iterate on the Rego logic in the **OPA Playground**.
2. Test with a **local PDP** and your real policy data.
3. **Commit the code to your GitOps repository**.
:::

## Next steps

- [Defining attributes](/how-to/build-policies/abac/defining-attributes): store attribute values or pass them in the check.
- [Write custom policies](/integrations/gitops/custom_policy): add your own Rego code to the policy repository.
- [Model ownership](/how-to/ownership): compare ABAC and ReBAC approaches to resource ownership.
