---
title: No roles for the user (no_user_roles)
description: "Fix a no_user_roles decision, where the user is synced to Permit but has no role assignment in any tenant."
---

A denied decision with the code `no_user_roles` means the user exists in Permit but has no role assignment in any tenant. This page is for developers who find this code in a decision log and need to assign a role to the user.

## What the no_user_roles reason means

The policy decision point (PDP) writes this code in the role-based access control (RBAC) part of the decision log's debug details (`debug.rbac`). The debug details appear when [Debug Mode](/how-to/use-audit-logs/debug-mode) is on.

| Field | Value |
|---|---|
| `code` | `no_user_roles` |
| `reason` | `no roles assigned to user '<user-key>'` |
| `support_link` | A link to `/errors/no_user_roles` on docs.permit.io |

The PDP runs its RBAC checks in a fixed order and reports the first one that fails. When you see `no_user_roles`, the resource type, the action, and the tenant exist, and the user is synced. The user has no roles in any tenant.

If the user has roles in other tenants but not in the tenant of the check, the code is [no_role_in_tenant](/how-to/use-audit-logs/errors/no_role_in_tenant) instead.

## Causes of no_user_roles

| Cause | How to confirm | Fix |
|---|---|---|
| Your app synced the user without role assignments. | The user has no roles in the **Directory** screen. | [Assign a role to the user](#assign-roles-to-a-user-with-the-permit-web-application). |
| Someone removed the user's last role assignment. | The user has no roles in the **Directory** screen, and had roles before. | [Assign a role to the user](#assign-roles-to-a-user-with-the-permit-web-application). |
| The check uses a different user key than the user that has roles, and both users exist. | The user key in the `reason` differs from the key of the user with roles. | Send the user key of the user with roles in `permit.check()`. |

## Fix no_user_roles \{#a-solution-to-the-issue}

### Assign a role in the Permit dashboard \{#assign-roles-to-a-user-with-the-permit-web-application}

1. Open the [Directory](https://app.permit.io/user-management) screen in the Permit dashboard.
2. Select the user and add a role assignment.
3. Choose the tenant that the check names and the role that grants the action, then save.

For a walkthrough with screenshots, see [Assign a role to the user](/how-to/build-policies/rbac/building-rbac-policy#assigning-a-role-to-a-user).

### Assign a role with the API \{#assign-roles-to-user-via-the-api}

Call the [Assign role](https://api.permit.io/v2/redoc#tag/Role-Assignments/operation/assign_role) endpoint with the `user`, `role`, and `tenant` keys.

### Assign a role with an SDK \{#assign-roles-to-user-via-the-sdk}

- [Assign a role with the Node.js SDK](/sdk/nodejs/role/assign-role)
- [Assign a role to a user in a tenant with the Python SDK](/sdk/python/usage-example#role-assignments)
- [All SDKs](/sdk/sdks-overview)

To assign roles when you sync the user, pass role assignments in the sync call. See [Sync users](/how-to/sync-users).

## Confirm the fix

1. Run the same `permit.check()` call again with the same user, action, resource, and tenant. To run the check from a terminal, use [`permit pdp check`](/how-to/permit-cli/permit-cli-pdp#permit-pdp-check).
2. Open the [Audit Log](https://app.permit.io/audit-log) and select the new decision.

The `debug.rbac.code` is no longer `no_user_roles`. If the code is `no_role_in_tenant` or `no_permission`, the role assignment exists, and the next check failed.

## Related pages

- [List role assignments](/how-to/enforce-permissions/list-role-assignments)
- [No roles for the user in this tenant (no_role_in_tenant)](/how-to/use-audit-logs/errors/no_role_in_tenant)
- [User not synced (user_not_synced)](/how-to/use-audit-logs/errors/user_not_synced)
