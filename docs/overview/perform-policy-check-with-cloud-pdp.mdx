---
sidebar_position: 3
title: Run your First Policy Check (Managed Cloud PDP)
timeline: true
description: "Call permit.check() against the managed Cloud PDP, see the check denied, change the policy, and see the same check allowed without a code change."
---

import CodeBlock from "../../src/components/code-block/CodeBlock";
import TimelineStep from "../../src/components/timeline/TimelineStep";
import TimelineWrapper from "../../src/components/timeline/TimelineWrapper";

Run your first permission check with `permit.check()` against the managed Cloud PDP, then change the policy in the Permit dashboard and see the same check return a different decision. This tutorial is for developers new to Permit.io who have configured a role-based access control (RBAC) policy.

The check goes to a policy decision point (PDP), the service that decides whether a user can perform an action on a resource. This tutorial uses the Cloud PDP, which Permit hosts at `https://cloudpdp.api.permit.io`, so you don't run any infrastructure.

:::info When to use a container PDP instead
The Cloud PDP does not support attribute-based access control (ABAC) policies. If you need ABAC, or you want checks to run inside your own network, follow [Run Local Authorization Microservice](/overview/local-authorization-microservice). For the full comparison, see [Cloud PDP capabilities](/concepts/pdp/cloud-pdp-capabilities).
:::

## Prerequisites

- An RBAC policy with a role, a resource, and a user assigned to the role. See [Configure your first RBAC policy](/overview/configure-your-first-rbac-policy).
- The Node.js SDK (`permitio`) installed, imported, and initialized with your environment API key and the Cloud PDP URL. See [Use the Permit API and SDK](/overview/use-the-permit-api-and-sdk).

## How permit.check() works \{#before-we-start-understanding-the-check-function}

`permit.check()` asks the PDP whether a user can perform an action on a resource, and returns `true` or `false`. Your code proceeds only when the result is `true`. Place the check at each point in your application that enforces the policy.

| Argument | Type | Description |
|---|---|---|
| `user` | string or object | The key of the user performing the action, or an object with the key and user attributes. |
| `action` | string | The action the user performs, such as `read` or `update`. |
| `resource` | string or object | The resource type key, such as `document`, or an object with the resource type, instance key, tenant, and attributes. |

This example checks whether the user `john@permit.io` can `read` a `document`:

<CodeBlock folderPath="/walkthroughs/first-policy-check/check-function" />

For tenants, attributes, relationships, and the `context` argument, see [Check permissions with permit.check()](/how-to/enforce-permissions/check).

<TimelineWrapper>
<TimelineStep>

## Review the policy in the Policy Editor \{#understanding-your-policy-configuration}

In the Permit dashboard, open **Policy** > **Policy Editor**. In this example, the `Admin` role can `create`, `delete`, `publish`, and `read` a `Document`. The `Admin` role cannot `update` a `Document`.

<img
  src="/img/updated/walkthroughs/first-policy-check/permissions-false.png"
  alt="Policy Editor with the Admin role allowed to create, delete, publish, and read Document, and the update checkbox cleared"
  data-zoomable
/>

</TimelineStep>
<TimelineStep>

## Review the user role assignments \{#reviewing-user-assignments}

Open **Directory**. In this example there are two users. Macy Smith, with the user key `user|987654321`, has the `Admin` role in the `default` tenant. John Smith has no role. The checks in this tutorial use Macy Smith's user key.

<img
  src="/img/updated/walkthroughs/first-policy-check/users-and-roles.png"
  alt="Directory screen listing John Smith with no role and Macy Smith with the Admin role in the default tenant"
  data-zoomable
/>

</TimelineStep>
<TimelineStep>

## Add the permission check to your code \{#setting-up-the-enforcement-point}

Add a check that asks whether Macy Smith can `update` a `document`:

<CodeBlock folderPath="/walkthroughs/first-policy-check/check-function-false" />

The check passes three values to the Cloud PDP:

- The user key, `user|987654321`
- The action, `update`
- The resource type, `document`

The PDP returns `true` if the policy allows the action and `false` if it does not.

</TimelineStep>
<TimelineStep>

## Run the check and see it denied \{#testing-initial-permissions}

Run your code. The check returns `false`, because the `Admin` role doesn't have the `update` permission on `Document`.

Open **Audit Log** in the Permit dashboard. The entry for `user|987654321`, action `update`, and resource `Document` shows the decision **DENIED**.

<img
  src="/img/updated/walkthroughs/first-policy-check/audit-log-fail.png"
  alt="Audit Log entry for user|987654321 performing update on Document in the default tenant, with the decision DENIED"
  data-zoomable
/>

</TimelineStep>
<TimelineStep>

## Allow the update action in the Policy Editor \{#updating-permissions}

In the **Policy Editor**, select the `update` checkbox for `Document` in the `Admin` row, and save the change. Every user with the `Admin` role, including Macy Smith, can update a `Document`.

<img
  src="/img/updated/walkthroughs/first-policy-check/permissions-true.png"
  alt="Policy Editor with the Admin role allowed to create, delete, publish, read, and update Document"
  data-zoomable
/>

</TimelineStep>
<TimelineStep>

## Run the check again and see it allowed \{#testing-updated-permissions}

Run the same check again, without changing your code:

<CodeBlock folderPath="/walkthroughs/first-policy-check/check-function-true" />

The check returns `true`. In **Audit Log**, the latest entry for the same user, action, and resource shows the decision **PERMITTED**.

<img
  src="/img/updated/walkthroughs/first-policy-check/audit-log-pass.png"
  alt="Audit Log entry for user|987654321 performing update on Document, with the decision PERMITTED"
  data-zoomable
/>

The decision changed because the policy changed. Your application code stayed the same.

</TimelineStep>
</TimelineWrapper>

## Next steps \{#what-did-you-learn}

Checks to the Cloud PDP travel over the internet to Permit. If your checks are latency-sensitive, run a PDP container next to your application, so checks don't make a network round trip to Permit.

- [Run the PDP container on your machine](/overview/run-pdp)
- [Perform a local policy check](/overview/perform-a-local-policy-check)
- [Sync your first user with the SDK](/overview/sync-your-first-user-with-sdk)
- [Check permissions with tenants, attributes, and relationships](/how-to/enforce-permissions/check)
