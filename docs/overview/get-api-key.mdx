---
sidebar_position: 2
title: Get your API key
description: "Copy your Permit.io environment API key from the dashboard so your application, SDK, and PDP can authenticate with Permit."
---

import CodeBlock from "../../src/components/code-block/CodeBlock";

Copy the environment API key that your application, the Permit SDK, and your policy decision point (PDP) use to authenticate with Permit.io. Each API key belongs to one environment, so copy the key of the environment your application connects to.

## Prerequisites

- A Permit.io account with a project and an environment. See [Projects and environments](/manage-your-account/projects-and-env).

## Copy the API key from the Projects screen \{#get-your-permit-environment-api-key}

1. Open the [Permit dashboard](https://app.permit.io/) and go to the **Projects** screen.
2. Find the [project](/overview/glossary#projects) and the [environment](/overview/glossary#environment) your application connects to.
3. On the environment card, click the ![three dots icon](/ui-videos/getting-started/connecting-your-app/dots.png) in the top-right corner.
4. Choose **Copy API Key** from the menu. The API key is copied to your clipboard.

<img
  src="/img/updated/quickstart/sdk-key-1.png"
  alt="Projects screen with the Production environment card menu open and Copy API Key highlighted"
  data-zoomable
/>

## Copy the API key from the user menu

You can also copy the API key of the active environment. Click your user icon in the top-right corner, then choose **Copy Environment Key**.

<img
  src="/img/updated/quickstart/sdk-key-2.png"
  alt="User menu open in the Permit dashboard with Copy Environment Key highlighted"
  data-zoomable
/>

:::info Copy Environment Key uses the active environment
**Copy Environment Key** copies the API key of the environment that is active in the sidebar. If you switch the active environment and click **Copy Environment Key** again, you copy the API key of the newly active environment, which is a different key.
:::

## Keep the API key secret

Anyone with an environment API key can call the Permit API for that environment, including changes to its policy. Load the API key from an environment variable or a secrets manager, and don't commit it to source control.

## Verify the API key

Send a request to the API key scope endpoint. Replace `[YOUR_ENV_API_KEY]` with the key you copied:

<CodeBlock folderPath="/walkthroughs/first-api-sdk/scope" />

A valid environment API key returns the IDs of the organization (workspace), project, and environment the key belongs to:

<CodeBlock folderPath="/walkthroughs/first-api-sdk/scope-result" />

## Next steps

- [Install and initialize the Permit SDK](/overview/use-the-permit-api-and-sdk)
- [Run the PDP container with your API key](/overview/run-pdp)
- [Rotate or manage API keys](/manage-your-account/workspace-settings#api-keys)
