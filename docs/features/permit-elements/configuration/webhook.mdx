# Configuring the Webhook

With some Permit Elements, you will get the option to configure a webhook to stay informed of the actions that are performed
by the end users.

:::info EXAMPLE
For the User Management Element, a webhook will allow you to recieve updates when a user invites someone new into the system.
:::

You will need to generate a new webhook URL that points to the running API application.
