---
sidebar_position: 3
title: Healthcare Demo Application
description: "Run the Galactic Health Corporation healthcare demo app and see how it combines RBAC, ABAC, and ReBAC policies in Permit.io, including time-limited caregiver access."
---

Run the Galactic Health Corporation (GHC) demo app and see how one healthcare app combines three permission models in Permit.io. This worked example is for developers who design authorization for apps with delegated access, such as patients who share records with caregivers.

GHC is a Next.js sample app set in the universe of "Rick and Morty". The app uses:

- **[Role-based access control (RBAC)](/how-to/build-policies/rbac/overview)** to show beta features to a pilot group.
- **[Relationship-based access control (ReBAC)](/how-to/build-policies/rebac/overview)** to let members share their health data with caregivers.
- **[Attribute-based access control (ABAC)](/how-to/build-policies/abac/overview)** to limit caregiver access to a date range.

:::note
The code is in the [Galactic-Health-Corporation repository](https://github.com/permitio/Galactic-Health-Corporation). For a longer walkthrough, read [Building healthcare authorization with Next.js](https://www.permit.io/blog/building-healthcare-authorization-nextjs) on the Permit blog.
:::

## Galactic Health Corporation features \{#galactic-health-corporation}

### GHC app key features \{#ghc-app-key-features}

| Feature | What it does in GHC |
|---|---|
| User roles | Doctors, patients, caretakers, and administrators each have a role with its own access level. |
| Patient management | Patients access their own health records, medical history, and treatment plans, and can grant limited access to caregivers or family members. |
| Doctor-patient relationships | Doctors view and manage the medical records of the patients under their care. |
| Attribute-based access | Attributes such as geographic location, patient age, date ranges, or treatment status restrict access to some resources. |
| Feature pilot group | Users in a pilot group see beta features. |
| Delegated permissions | Users delegate limited access to their health information to others, such as caregivers. |

## Run GHC locally \{#getting-started}

### Prerequisites

- **Node.js.** See [Node.js downloads](https://nodejs.org/en/download).
- **Docker**, to run the policy decision point (PDP) container. See [Install Docker Engine](https://docs.docker.com/engine/install/).
- **A Clerk account.** GHC uses [Clerk](https://clerk.com/) to authenticate users.
- **A Permit.io account and its environment API key.** See [Get your API key](/overview/get-api-key).

### Set up the application \{#setting-up-the-application}

1. Clone the repository and change into it:

   ```bash
   git clone https://github.com/permitio/Galactic-Health-Corporation.git
   cd Galactic-Health-Corporation
   ```

2. In the root folder, create a file named `.env.local` and copy the content of `.env.example` into it:

   ```bash
   NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
   NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
   NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
   NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

   NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="<your clerk publishable key>"
   CLERK_SECRET_KEY="<your clerk secret key>"
   PERMIT_SDK_KEY="<your permit sdk key>"
   PERMIT_PDP_URL=http://localhost:7766
   ```

3. In the Clerk dashboard, open **API Keys**, choose the Next.js example, and copy the `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` values into `.env.local`.
4. Copy your Permit environment API key into the `PERMIT_SDK_KEY` value in `.env.local`. `PERMIT_SDK_KEY` is the variable name the app reads; the value is the environment API key.
5. Run `npm install` to install the dependencies.

### Seed the policy in Permit \{#configuration-setup}

From the repository root, run `npm run config`. The command runs `scripts/setupPermit.js`, which creates the resources, actions, and roles that GHC uses in your Permit environment.

**Verify:** In the Permit dashboard, open the **Policy** screen. The resources from the [resource table](#entities) appear on the **Resources** tab.

### Start the PDP and the app \{#running-the-application}

1. Start the PDP container. Replace `<YOUR_PERMIT_API_KEY>` with your environment API key. The command maps the PDP's port 7000 to port 7766 on your machine, which matches `PERMIT_PDP_URL`.

   ```bash
   docker run -it \
     -e PDP_API_KEY=<YOUR_PERMIT_API_KEY> \
     -p 7766:7000 \
     -p 8081:8081 \
     permitio/pdp-v2:latest
   ```

2. In a new terminal, from the repository root, run `npm run dev`.
3. Open `http://localhost:3000`. The GHC sign-in page appears.

## RBAC and ReBAC model \{#permission-models---rbac-and-rebac}

GHC builds most of its policy with ReBAC, and uses RBAC for the pilot group.

### Entities

| Entity | Definition |
|---|---|
| Resource | A type of object you manage access to. Each resource can have many instances. |
| Action | An operation a user can perform on a resource. The policy allows or denies each action. |
| Relationship | A link between two resource instances, such as a member and the member's medical records. |
| Resource role | A role assigned on a specific resource instance. |
| Role derivation | A rule that grants a role on a related resource instance. In GHC, roles on a member derive roles on the member's profile, health plan, and medical records. |

### ReBAC requirements \{#requirements}

- Users can view their own profile details, health plans, and medical records.
- Users can view the profiles of other users they have a relationship with, such as family members.
- Users can delegate permission to view their health plan and medical records to other users, for a specific date range.
- Users can view some features in the app based on their pilot group.

The following table lists the resources, actions, relationships, and roles in the GHC model:

| Resource                | Actions      | Relationships                                    | Resource Roles                              | Derived Roles                             | Description                                      |
| ----------------------- | ------------ | ------------------------------------------------ | ------------------------------------------- | ----------------------------------------- | ------------------------------------------------ |
| Member                  | Write, Read  | Parent of: Medical Records, Health Plan, Profile | Owner (read, write)                         | Caregiver (read)                          | The root entity for all member-related resources |
| Profile                 | Write, Read  | Child of Member                                  | Owner (read, write)                         | Member:Caregiver, Member Group:Org Member | A member profile                                 |
| Health Plan             | Write, Read  | Child of Member                                  | Owner (read, write)                         | Member:Caregiver                          | A member's health plan                           |
| Medical Records         | Write, Read  | Child of Member                                  | Owner (read, write)                         | Member:Caregiver                          | A member's medical records                       |
| Member Group            | List, Assign |                                                  | Admin (list, assign), Org member (list)     |                                           | A member group                                   |
| New feature pilot group | View         |                                                  | Pilot group member (general role)           |                                           | A member group that has access to beta features  |

### API endpoints

GHC exposes the following endpoints. Each endpoint checks the listed roles or derived roles.

| Endpoint                                        | Description                                    | Roles                       | Derived Roles                                                         |
| ----------------------------------------------- | ---------------------------------------------- | --------------------------- | --------------------------------------------------------------------- |
| `GET /account/dashboard/profile/{user}`         | Get the current user's details                 | `Profile:Caregiver`         | `Member:Owner#Profile`<br/>`Member Group:Org Member#Profile`          |
| `GET /account/dashboard/health-plan/{user}`     | Get the current user's health plan             | `Health Plan:Caregiver`     | `Member:Owner#Health Plan`<br/>`Member:Caregiver#Health Plan`         |
| `GET /account/dashboard/medical-records/{user}` | Get the current user's medical records         | `Medical Records:Caregiver` | `Member:Owner#Medical Records`<br/>`Member:Caregiver#Medical Records` |
| `GET /account/member`                           | Get all the members from all the member groups | `Member Group:Org Member`   |                                                                       |
| `GET /account/caregiver`                        | Get all the user's caregivers                  | `Member:Owner`              |                                                                       |
| `POST /account/caregiver`                       | Add a caregiver to the user                    | `Member:Owner`              |                                                                       |
| `DELETE /account/caregiver`                     | Remove a caregiver from the user               | `Member:Owner`              |                                                                       |

## Test the caregiver flow \{#testing-the-application-flow}

Test the RBAC and ReBAC model with three users: Rick, Morty, and Bird Person.

1. Sign up for three GHC accounts: `rick@sanchez.app`, `morty@smith.app`, and `bird@person.app`.

   :::note
   To use other email addresses, edit the user keys in the [Permit Directory](https://app.permit.io/user-management).
   :::

2. Sign in as `rick@sanchez.app`. Rick's dashboard shows only Rick's own data, because each user owns their own member data.
3. Open the **Delegate Permissions Wizard**. The wizard lists only the users in Rick's member groups: Morty and Bird Person.
4. Share all the data with Morty, and assign Morty as a caregiver. You can limit Morty's access to a date range.
5. Sign in as Morty. In the **Shared Access** section, Morty sees Rick's health plan and medical records. Morty's access comes from the `Member:Caregiver` role.
6. Sign in as Bird Person. Bird Person sees only his own data, because he has no caregiver relationship with another user.

The flow demonstrates these permissions:

| User | What the user sees | Why |
|---|---|---|
| Rick | All of his own health data | Rick has the `Owner` role on his member data. |
| Morty | Rick's health plan and medical records | Rick assigned Morty the `Member:Caregiver` role. |
| Bird Person | Only his own health data | Bird Person has no relationship with other users. |
| Rick (pilot group) | A dashboard box that only the pilot group sees | Rick has the `Feature testing pilot group` role (RBAC). Other users don't see the box. |

## ABAC model for time-limited delegation \{#permission-models---abac}

### ABAC requirements \{#requirements-1}

A delegation must be limited to a date range. For example, Rick can give Morty access to his health plan and medical records for the next 30 days only.

### Date boundaries on role assignments \{#model}

Morty's instance-level role comes from the relationship between Rick and Morty. The relationship alone can't expire, so GHC adds date attributes that bound each role assignment. For example, if Morty has the `Member:Caregiver` role with `start_date` set to `2024-01-01` and `end_date` set to `2024-01-31`, Morty can view Rick's health plan and medical records only between those dates.

GHC stores the bounds in a `caregiver_bounds` user attribute on the caregiver. Each key in `caregiver_bounds` is the resource instance the caregiver was granted access to, and each value holds an RFC 3339 `start_date` and `end_date`. The `POST /api/account/caregiver` handler writes this attribute with `permit.api.users.update()` before it assigns the `caregiver` role:

```json
{
  "key": "user_morty_smith",
  "attributes": {
    "caregiver_bounds": {
      "health_plan:health_plan_user_rick_sanchez": {
        "start_date": "2030-05-01T09:00:00.000Z",
        "end_date": "2030-05-08T09:00:00.000Z"
      },
      "medical_records:medical_records_user_rick_sanchez": {
        "start_date": "2030-05-01T09:00:00.000Z",
        "end_date": "2030-05-08T09:00:00.000Z"
      }
    }
  }
}
```

The custom Rego rule in `scripts/custom.rego` allows the derived caregiver role only while the current time falls inside those bounds:

```rego
enforce_boundries(resource) {
	time.now_ns() >= time.parse_rfc3339_ns(abac.attributes.user.caregiver_bounds[resource].start_date)
	time.now_ns() <= time.parse_rfc3339_ns(abac.attributes.user.caregiver_bounds[resource].end_date)
}
```

`time.parse_rfc3339_ns` rejects any value that is not an RFC 3339 timestamp, so write the dates in the format shown above. The **Delegate Permissions Wizard** in GHC produces them with `Date.prototype.toISOString()`.

### Implementation options \{#implementation}

You can implement this ABAC rule in Permit in two ways:

| Option | How it works |
|---|---|
| Resource set in the Permit dashboard | Define a resource set with the date condition on the **ABAC Rules** tab. |
| Custom Rego policy | Write Rego code that Permit combines with the generated policy and the PDP evaluates. |

GHC uses the custom Rego option. The generated and custom policy code is in the [ghc-demo-policy repository](https://github.com/permitio/ghc-demo-policy).

### Configure GitOps

To write custom policy code, connect Permit to a Git repository with GitOps. Permit stores the generated policy in the repository, and you change the policy through branches and pull requests. See [Git and Permit](/integrations/gitops/github).

### Add the custom policy \{#configure-the-policy}

In the branch of your policy repository that Permit syncs for your environment:

1. Replace the content of `custom/root.rego` with the content of `scripts/custom.rego` from the GHC repository.
2. Edit the top-level `root.rego` file. By default, `root.rego` has two `allow` rules, one for `policies.allow` (the generated policy) and one for `custom.allow`, so access is allowed if either rule allows it. Remove the second `allow` rule, and put both conditions in one `allow` rule so that both must pass:

   ```rego
   allow {
   	policies.allow
   	custom.allow
   }
   ```

To learn more about custom Rego in Permit, see [Write custom policies](/integrations/gitops/custom_policy).

### Test the time-limited delegation \{#application-flow}

Run the caregiver flow from [Test the caregiver flow](#testing-the-application-flow) again, and set a date range in the **Delegate Permissions Wizard**. Morty can view Rick's data only while today's date is inside the range.

## Hosted app

A hosted instance of GHC runs at [ghc.up.railway.app](https://ghc.up.railway.app/). Sign in with one of the following users to try the flows without running the app locally.

:::warning Shared demo accounts
These credentials are public, and anyone can sign in with them. Changes you make to permissions in the hosted app are visible to every other visitor. Don't enter real personal or health data.
:::

| User              | Password  | Groups                  |
| ----------------- | --------- | ----------------------- |
| rick@sanchez.app  | Aa123456! | ricks_org, smith_family |
| morty@smith.app   | Aa123456! | smith_family            |
| bird@person.app   | Aa123456! | ricks_org               |
| homer@simpson.app | Aa123456! |                         |

## Next steps

- [Build ReBAC policies](/how-to/build-policies/rebac/building-rebac-policies) with resource roles and role derivations.
- [Write custom policies](/integrations/gitops/custom_policy) in Rego alongside the generated policy.
- [Teleco Demo Application](/modeling/pink-mobile) for a policy model built with the Node.js SDK.
- [Check permissions with permit.check()](/how-to/enforce-permissions/check) for the check options the app uses.
