# deleteUser

Delete a user with specific user ID.

### Parameters

`user_id` - The ID of the user. This can also be the user email.<br/>

### Implementation

```python
response = await permit.api.delete_user(user_id)
```
