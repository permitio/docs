# getRole

Get a single tenant role, if such role exists.

### Parameters

`role_id` - The ID of the role. This can also be the role name.<br/>

### Implementation

```python
roles = await permit.api.get_role(role_id)
```
