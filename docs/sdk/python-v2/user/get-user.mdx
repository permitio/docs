# getUser

Get the specificed user and their information.

### Parameters

`user_id` - The ID of the user. This is the unique key of the user.<br/>

### Implementation

```python
user = await permit.api.get_user(user_id)
```
