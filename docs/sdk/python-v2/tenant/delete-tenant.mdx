# deleteTenant

Deletes a tenant.

### Parameters

`tenant_id` - The ID of the tenant. This can also be the tenant name.<br/>

### Implementation

```python
response = await permit.api.delete_tenant(tenant_id)
```
