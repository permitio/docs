# deleteResource

Deletes a resource with the corresponding ID.

### Parameters

`resource_id` - The unique resource key or the resource name.<br/>

### Implementation

```python
response = await permit.api.delete_resource(resource_id)
```
