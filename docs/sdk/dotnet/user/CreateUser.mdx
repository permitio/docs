---
title: Create a user with the .NET SDK
sidebar_label: CreateUser
description: "Reference for permitClient.Api.CreateUser() in the .NET SDK, which creates a user that you can run permission checks on."
---

`permitClient.Api.CreateUser()` creates a user in the environment that your API key belongs to, so you can run permission checks on that user. This reference is for .NET developers who sync users from backend code. To create a user or replace the user if the key already exists, use [`SyncUser`](/sdk/dotnet/user/SyncUser) instead.

## `CreateUser` signature

`public async Task<UserRead> CreateUser(UserCreate user)`

## `CreateUser` parameters \{#parameters}

| Parameter | Type | Required | Description |
|---|---|---|---|
| `user` | `UserCreate` | Yes | The user to create. The `UserCreate` class is in the `PermitSDK.OpenAPI.Models` namespace. |

## `UserCreate` properties \{#payload}

| Property | Type | Required | Description |
|---|---|---|---|
| `Key` | `string` | Yes | A unique ID by which Permit identifies the user. You pass the same key as the user in `permit.Check()`. Use any value that is unique in your system, such as a user ID from your identity provider. |
| `Email` | `string?` | No | The email of the user. |
| `First_name` | `string?` | No | The first name of the user. |
| `Last_name` | `string?` | No | The last name of the user. |
| `Attributes` | `object?` | No | User attributes that attribute-based access control (ABAC) policies evaluate, as key-value pairs. |

Build a `UserCreate` object named `userObj`. Replace the placeholder values with the details of your user:

```dotnet
var userObj = new UserCreate{
    Key = "key",
    Email = "email@example.com",
    First_name = "John",
    Last_name = "Smith",
    Attributes = {}
};
```

## Example: create a user with `CreateUser` \{#implementation}

The example uses a client named `permitClient`, created with `new Permit(...)` as shown in [Check permissions with the .NET SDK](/sdk/dotnet/quickstart-dotnet):

```dotnet
var response = await permitClient.Api.CreateUser(userObj);
```

## `CreateUser` return value and errors

On success, `CreateUser` returns a `UserRead` with the user's `Key`, `Id`, `Email`, `First_name`, `Last_name`, and `Attributes`.

If the Permit API returns an error, `CreateUser` throws a `PermitApiException`. The exception's `StatusCode` property has the HTTP status, and its `Response` property has the response body. The Permit API returns 409 when a user with the same key already exists in the environment.

## Related user methods

- [Sync a user with the .NET SDK](/sdk/dotnet/user/SyncUser)
- [Assign a role to a user with the .NET SDK](/sdk/dotnet/role/AssignRole)
- [Get a user with the .NET SDK](/sdk/dotnet/user/GetUser)
- [Sync users and assign roles](/how-to/sync-users)
