# deleteResource

Deletes a resource with the corresponding key or id.

### Parameters

`resource_id` - The unique resource key or the resource id.<br/>

### Implementation

```python
response = await permit.api.delete_resource(resource_id)
```
