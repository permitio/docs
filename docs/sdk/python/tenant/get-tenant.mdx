# getTenant

Fetch the tenant and the tenant information.

### Parameters

`tenant_id` - The ID of the tenant. This can also be the tenant name.<br/>

### Implementation

```python
tenant = await permit.api.get_tenant(tenant_id)
```
