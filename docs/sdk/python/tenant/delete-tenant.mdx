# deleteTenant

Deletes a tenant.

### Parameters

`tenant_id` - The id of the tenant. This can also be the tenant key.<br/>

### Implementation

```python
response = await permit.api.delete_tenant(tenant_id)
```
