---
sidebar_position: 6
title: Check permissions with the Python SDK (sync)
description: "Get your API key, run a PDP, install the sync Python SDK, and call permit.check() from a Python application that doesn't use asyncio."
sidebar_class_name: hidden
---

Connect a Python application that doesn't use `asyncio` to Permit.io and call `permit.check()` to allow or deny a request. This quickstart is for backend developers who have a Permit.io policy and want to enforce the policy from synchronous Python code, for example a Flask app. At the end, a Flask app returns an allow or deny result for each request, and the check appears in the Permit audit log. If your code uses `asyncio`, see [Check permissions with the Python SDK (asyncio)](/sdk/python/quickstart-python).

import IntroContent from "@site/docs/getting-started/_quickstart-parts/_quickstart_intro.mdx";
import SyncPythonExample from "@site/docs/getting-started/_quickstart-parts/_quickstart_python_sync.mdx";

<IntroContent />

## 3. Install the sync Python SDK and check permissions

<SyncPythonExample />

## 4. Confirm the check in the audit log

Open the **[Audit Log](https://app.permit.io/audit-log)** screen in the Permit dashboard. Each permission check from your application appears as an entry with the user, the action, the resource, and the decision.

If the check doesn't appear in the audit log, see [Troubleshoot audit logs](/how-to/use-audit-logs/troubleshooting).

## Next steps

- [Sync users and assign roles](/how-to/sync-users) so checks evaluate your real users.
- [Check permissions with permit.check()](/how-to/enforce-permissions/check) with tenants, attributes, and relationships.
- [Python SDK examples](/sdk/python/usage-example)
- [Deploy the PDP to production](/how-to/deploy/deploy-to-production).
- [Compare SDK features by language](/sdk/sdks-overview).
