# getRole

Get a single tenant role, if such role exists.

### Parameters

`role_id` - The id of the role. This can also be the role key.<br/>

### Implementation

```python
roles = await permit.api.get_role(role_id)
```
