# permit.api.roles.list()

Fetch a list of the available roles.

### Parameters

`page` - The page number of the results to fetch, starting at 1.<br/>
`perPage` - The number of results per page (default is 100).<br/>

### Implementation

```java
RoleRead[] roles = permit.api.roles.list();
```
