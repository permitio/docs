# permit.api.resources.list()

Fetch a list of the available resources.

### Parameters

`page` - The page number of the results to fetch, starting at 1.<br/>
`perPage` - The number of results per page (default is 100).<br/>

### Implementation

```java
ResourceRead[] resources = permit.api.resources.list();
```
