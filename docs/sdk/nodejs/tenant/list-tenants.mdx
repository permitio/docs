# listTenants

Fetch a list of the available tenants.

### Parameters

`page` - The page number of the results to fetch, starting at 1. <br/>

### Implementation

```js
const tenantList = await permit.api.listTenants(page);
```
