# getTenant

Fetch the tenant and the tenant information.

### Parameters

`tenantId` - The ID of the tenant. This can also be the tenant name.

### Implementation

```js
const tenant = await permit.api.getTenant(tenantId);
```
