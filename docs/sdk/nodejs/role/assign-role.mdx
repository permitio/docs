# assignRole

Assign a role to a specific user.

### Parameters

`assignedRole` - RoleAssignmentCreate <br/>

### Implementation

```js
const response = await permit.api.assignRole(${JSON.stringify(assignedRole)});
```

### Response Example

```json
{}
```
