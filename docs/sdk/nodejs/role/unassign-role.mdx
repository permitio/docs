# unassignRole

Unassign a role from a specific user.

### Parameters

`removeRole` - RoleAssignmentRemove <br/>

### Implementation

```js
const response = await permit.api.unassignRole(${JSON.stringify(removedRole)});
```

### Response Example

```json
{}
```
