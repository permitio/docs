# deleteUser

Get the specificed user information.

### Parameters

`userId` - The ID of the user. This can also be the user key (email or other). <br/>

### Implementation

```js
const response = await permit.api.deleteUser(userId);
```

### Response Example

```json
{}
```
