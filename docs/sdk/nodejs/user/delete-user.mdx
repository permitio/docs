# deleteUser

Delete a user with specific user ID.

### Parameters

`userId` - The ID of the user. This can also be the user email.<br/>

### Implementation

```js
const response = await permit.api.deleteUser(userId);
```
