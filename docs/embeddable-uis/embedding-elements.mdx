---
sidebar_position: 5
title: Embed Permit Elements
sidebar_label: Embedding Elements
description: "Configure JWKS, create and customize an element in the Permit dashboard, add its iframe to your application, and log users in with the frontendOnly method."
---

Embed a Permit element in your web application, from the Permit dashboard to a signed-in user who sees the element. This page is for frontend developers adding Permit Elements, the embeddable UI components, to an application. The example embeds a User Management element and logs users in with the `frontendOnly` method, which needs no backend route.

For a working application, see the [permit-demo-element example project](https://github.com/permitio/permit-demo-element) on GitHub.

## How embedding works \{#overview}

You create and configure the element in the Permit dashboard, and copy its iframe code into your application. Before the iframe loads, your frontend logs the user in to the element with `permit.elements.login()`. The element then shows the data and actions that the user's role allows.

![Login and load flow for Permit Elements: permit.elements.login() calls your backend login route or Permit's frontend-only login endpoint, the element iframe loads, and the element requests /me and /runtime from the Permit API](/img/elements/flow-elements.png)

## Prerequisites

- A Permit.io account with roles defined in your policy. See [Configure your first RBAC policy](/overview/configure-your-first-rbac-policy).
- Your users synced to Permit, each with a role in the tenant the element shows. See [Sync users](/how-to/sync-users).
- An identity provider that issues JSON Web Tokens (JWTs) for your users, and the JSON Web Key Set (JWKS) that verifies them. See [What are JWKS?](/how-to/enforce-permissions/url-mapping/fetching-jwks#what-are-jwks).
- A frontend project that installs packages with npm.

## 1. Configure JWKS for your environment \{#configuring-jwks-for-your-environment}

The `frontendOnly` login method sends the user's JWT to Permit, and Permit verifies the JWT with the JWKS of the environment.

1. In the Permit dashboard, open **Settings** and click **JWKS Config**.
2. Select the environment the element runs in.
3. Paste your JWKS in the editor and click **Save**.

![JWKS Config tab in the Permit dashboard settings, with a JWKS pasted in the editor](/img/elements/user-management/ConfigureJwks.png)

Check that each user's key in Permit matches the `sub` claim of the user's JWT. The **Users** page in the Permit dashboard shows user keys. To read the `sub` claim, decode a JWT with a tool such as [jwt.io](https://jwt.io/). If the user key is in a different claim, pass that claim's name as `userKeyClaim` when you log the user in.

For JWKS options, including a JWKS URL, see [Configure JWKS](/how-to/enforce-permissions/url-mapping/configuring-jwks).

## 2. Create the element \{#creating-a-new-element}

1. Open the [Elements](https://app.permit.io/elements) screen in the Permit dashboard.
2. Under **User Management**, click **Create Element**.

   ![Elements screen with the Create Element button under User Management](/img/elements/user-management/CreateElement.png)

3. Fill in the element details:

   | Field | Description |
   |---|---|
   | **Name** | The name of the element. |
   | **Permission Model** | RBAC (role-based access control) or ReBAC (relationship-based access control). |
   | **Roles Levels** | Which of your roles belongs to each permission level. See [Permission levels](/embeddable-uis/permission-levels). |

   ![New User Management element form with the name, permission model, and role levels fields](/img/elements/user-management/ElementsFormEdit.png)

## 3. Customize the element \{#customize-the-element}

In the same form, set how the element looks and behaves:

- **Colors:** set the background color and primary color to match your brand.
- **Title:** give the element a title your end users recognize, or hide the title.
- **Add User field:** show or hide the field. Hiding the field changes only the display, and doesn't change what users can do.
- **User details:** show the user's email, full name, or both.
- **Webhook:** send a request to your server when users act in the element. See [Configure webhooks](/embeddable-uis/webhooks).

![Element settings panel with color, title, user detail, and webhook options](/img/elements/user-management/element-settings.png)

## 4. Preview the element \{#previewing-the-element}

The **End User Preview** on the right of the form shows the element as an end user sees it. To preview a specific permission level or tenant, see [Preview an element](/embeddable-uis/user-preview).

![End User Preview panel next to the element form](/img/elements/user-management/ElementPreview.png)

Click **Save**. Permit creates the element and opens the element's page.

## 5. Add the iframe to your application \{#copy-the-iframe-code}

1. On the element's page, click **Generate Code**.
2. In the **Element Embed Code** dialog, click **Copy Code**.

   ![Element Embed Code dialog with the iframe snippet and the Copy Code button](/img/elements/user-management/EmbedCode.png)

3. Paste the iframe into the page of your application where the element appears.

The iframe `src` contains the environment ID as `envId`. You need that ID in step 7.

## 6. Install permit-js \{#installing-permit-js}

Install the `@permitio/permit-js` package in your frontend project. The package provides `permit.elements.login()` and `permit.elements.logout()`.

```bash
npm install @permitio/permit-js
```

## 7. Log the user in with frontendOnly \{#client-side-login-method-using-frontendonly}

Call `permit.elements.login()` after your identity provider signs the user in, and before the element iframe renders. Import the package with `import permit, { LoginMethod } from "@permitio/permit-js"`, and set these parameters:

| Parameter | Value |
|---|---|
| `loginMethod` | `LoginMethod.frontendOnly` |
| `userJwt` | The signed-in user's JWT. |
| `tenant` | The key of the tenant the element shows. The user must have a role in this tenant. |
| `envId` | The environment ID from the iframe `src`, or from the [List Environments API](https://api.permit.io/v2/redoc#tag/Environments/operation/list_environments). The environment must have the JWKS from step 1. |
| `userKeyClaim` | Optional. The JWT claim that holds the user key, when the key is not in `sub`. |

Replace `<YOUR_USER_JWT>`, `<YOUR_TENANT_KEY>`, and `<YOUR_ENV_ID>` with your values:

```ts
permit.elements
  .login({
    loginMethod: LoginMethod.frontendOnly,
    userJwt: "<YOUR_USER_JWT>",
    tenant: "<YOUR_TENANT_KEY>",
    envId: "<YOUR_ENV_ID>",
  })
  .then((res: any) => {
    console.log("success", res);
  })
  .catch((err: any) => {
    // you can handle the error either here or in your BE
    console.log("err", err);
  });
```

For the other login methods, which use a backend route, see [Log users in to Permit Elements](/embeddable-uis/element-login).

## 8. Log the user out \{#client-side-logout-method}

Call `permit.elements.logout()` in the code that signs the user out of your application. If you skip the call, the element session stays active in that browser after the user signs out.

```javascript
permit.elements.logout();
```

## Verify the element loads

1. Sign in to your application as a user whose role is in a visible permission level.
2. Open the page with the element. In the browser's developer tools, the **Network** tab shows a successful login request, and a cookie named `permit_session` is set.
3. The element shows the users of the tenant you passed as `tenant`.

## Login errors \{#login-errors}

If the login fails, Permit returns an error such as `USER_NOT_FOUND` or `TENANT_NOT_FOUND`. For every error, its cause, and its fix, see [Login errors](/embeddable-uis/element-login#login-errors). For network and cookie problems, see [Troubleshoot Permit Elements](/embeddable-uis/troubleshooting).

## Next steps

- [Assign roles to permission levels](/embeddable-uis/permission-levels) to control what each user can do in the element.
- [Configure webhooks](/embeddable-uis/webhooks) to get notified when users act in the element.
- [Set up email invitations](/embeddable-uis/email-configuration-and-templates) for the User Management element.
- [Embed the Access Request element](/embeddable-uis/element/access-request) so users can ask for access.
