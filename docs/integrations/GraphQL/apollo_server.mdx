---
sidebar_position: 2
title: Add Permit permission checks to Apollo Server
sidebar_label: Apollo Server
description: "Add an Apollo Server plugin that maps each GraphQL operation to a Permit resource and action, and rejects requests the policy doesn't allow."
---

Check a Permit.io permission for every GraphQL operation in Apollo Server, with one server plugin. This page is for backend developers who run Apollo Server and want to block operations a user isn't allowed to run. The example builds on the server in [Apollo's fullstack tutorial](https://github.com/apollographql/fullstack-tutorial/tree/main/final/server), which uses Apollo Server 3. To compare this approach with checks in resolvers, schema directives, or data sources, see [Enforce permissions in a GraphQL API](/integrations/GraphQL/overview).

## How the plugin works \{#apollo-server}

1. A client sends a GraphQL operation to Apollo Server.
2. The Permit plugin turns the operation into a resource and an action.
3. The plugin calls `permit.check()` with the user key, the action, and the resource.
4. If the policy denies the operation, the plugin throws a `Not allowed` error and Apollo Server doesn't run the operation.

## Prerequisites

- A clone of the [fullstack tutorial server](https://github.com/apollographql/fullstack-tutorial/tree/main/final/server), or your own Apollo Server 3 project
- Your environment API key ([Get your API key](/overview/get-api-key))
- A function in your server that returns the user key from the request's verified token. The examples call it `getUserIdFromJWT()`; replace it with your own function.

## 1. Add the Permit SDK to your project \{#1-add-the-permit-package-to-your-project}

Install the `permitio` package: `npm install permitio`

## 2. Import the Permit SDK in your server \{#2-import-the-package-into-your-server}

Add the import to your server file: `const { Permit } = require("permitio");`

## 3. Create the Permit client \{#3-init-the-permit-object-with-the-secret-api-key}

Create a Permit client, and replace `permit_key_secret` with your environment API key. The Permit dashboard also shows this code, with your API key filled in, on the [Connect an SDK](https://app.permit.io/get-started/connect-an-sdk) screen.

```js
const permit = new Permit({
  // in production, you might need to change this url to fit your deployment
  pdp: "https://cloudpdp.api.permit.io",
  // your api key
  token: "permit_key_secret"
});
```

The `pdp` URL points to the Cloud PDP, the policy decision point (PDP) that Permit hosts. In production, set `pdp` to the URL of the PDP you deploy. See [Deploy the PDP to production](/how-to/deploy/deploy-to-production).

:::warning Keep the API key out of source control
Anyone with your environment API key can change the policy of that environment through the Permit API. Load the key from an environment variable in production, and don't commit it.
:::

## 4. Create the Permit plugin

Choose one of two ways to map operations to resources and actions.

### Option 1: map operation names to resources and actions \{#4-option-1---operation-name-to-resource--action-mapping}

This plugin looks up each operation name in a `PermissionMap` object and checks the mapped resource and action. The plugin reads `operationName` from the request, so clients must send named operations. An operation that isn't in the map is denied.

```js
const PermissionMap = {
  "login": {resource: "user", action: "login"},
  "logout": {resource: "user", action: "logout"},
  "me": {resource: "user", action: "get"},
  "launches": {resource: "launch", action: "getall"},
  "getlaunch": {resource: "launch", action: "get"},
}

const permitPlugin = {
  async requestDidStart(context) {
    const operationName = context.request.operationName || "";
    var userId = await getUserIdFromJWT("");
    let allowed = false;
    if (operationName.toLowerCase() in PermissionMap) {
      const { resource, action } = PermissionMap[operationName.toLowerCase()];
      allowed = await permit.check(userId, action, resource);
    }
    else {
      console.warn('No such operation in PermissionMap', operationName);
    }
    if (!allowed) {
      throw new Error("Not allowed");
    }
  },
};
```

In the Permit Policy Editor, create each resource in the map (`user`, `launch`) with its actions (`login`, `logout`, `get`, `getall`), and give roles permission to them.

### Option 2: use the operation name as the resource and the operation type as the action \{#4-option-2---operation-name-as-resource-and-operation-type-as-action}

This plugin uses the lowercase operation name as the resource. The action is `write` for a mutation and `read` for any other operation. The check runs in the `didResolveOperation` hook, after Apollo Server has parsed the operation.

```js

const permitPlugin = {
  requestDidStart(requestContext) {
    return {
      async didResolveOperation (context) {
        const op = context.operationName
        var userId = await getUserIdFromJWT("");
        const isMutation = context.operation.operation === 'mutation'
        const allowed = await permit.check(userId, isMutation? "write": "read", op.toLowerCase()) // this will look like "action: write, resource:launches" or "action: read, resource:launches"
        if (!allowed) {
          throw new Error("Not allowed");
        }
      },
    }
  },
}
```

In the Permit Policy Editor, create a resource for each operation name, for example `launches`, with `read` and `write` actions.

## 5. Add the plugin to Apollo Server \{#5-add-the-plugin-to-the-apollo-server-plugins-list}

Add `permitPlugin` to the `plugins` list of the Apollo Server constructor:

```js
// Set up Apollo Server
const server = new ApolloServer({
  debug: true,
  typeDefs,
  resolvers,
  dataSources,
  context,
  introspection: true,
  apollo: {
    key: process.env.APOLLO_KEY,
  },
  plugins: [ApolloServerPluginLandingPageLocalDefault({ embed: true }), permitPlugin]
});
```

## 6. Verify the permission checks

1. Start the server. The fullstack tutorial server listens on `http://localhost:4000`.
2. In the Permit dashboard, assign a role with permission for the `launches` operation's resource and action to a user, and send a named `launches` query as that user. The server returns the query result.
3. Send the same query as a user without that permission. The response contains the `Not allowed` error.
4. Open the [Audit Log](https://app.permit.io/audit-log) screen in the Permit dashboard. Each check appears with the user, the action, the resource, and the decision.

## Next steps

- [Enforce permissions in a GraphQL API](/integrations/GraphQL/overview)
- [Check permissions with permit.check()](/how-to/enforce-permissions/check)
- [Configure your first RBAC policy](/overview/configure-your-first-rbac-policy)
- [Deploy the PDP to production](/how-to/deploy/deploy-to-production)
