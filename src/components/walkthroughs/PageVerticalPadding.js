/* eslint-disable spellcheck/spell-checker */
import React from "react";

const PageVerticalPadding = ({ children }) => {
  return <div className="py-8 lg:py-12">{children}</div>;
};

export default PageVerticalPadding;
